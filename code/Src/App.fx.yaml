App As appinfo:
    BackEnabled: =false
    OnStart: |+
        =If(
            !IsBlank(
                LookUp(
                    Administrators,
                    Title = User().Email
                )
            ),
            Set(
                is_admin,
                true
            ),
            Set(
                is_admin,
                false
            )
        )
        

