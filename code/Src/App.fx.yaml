App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =If(
            LookUp(
                Users,
                email = User().Email,
                userRole
            ) = "admin",
            Set(
                is_admin,
                true
            ),
            Set(
                is_admin,
                false
            )
        )

